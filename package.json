{
  "type": "module",
  "scripts": {
    "server": "npm install --no-package-lock && node server/main.js",
    "build": "npx esbuild docs/js/generate_story.js --bundle --outfile=src/generate_story.bundle.js --format=esm"
  },
  "dependencies": {
    "amnesic-openai": "1.0.*",
    "cute-con": "1.1.*",
    "express": "4.19.*",
    "html-piercer": "0.1.*",
    "multer": "1.4.*"
  }
}
